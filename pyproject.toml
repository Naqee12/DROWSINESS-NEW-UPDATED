[project]
name = "driveguard-ai"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "flask",
    "opencv-contrib-python",
    "mediapipe",
    "ultralytics",
    "torch",
    "numpy",
]

[tool.vercel]
entrypoint = "web_app:app"
